// Problem: Allocate Lowest 16-bit Number
// You are designing a system to manage the allocation of 16-bit unsigned integers (ranging from 0 to 65535). These numbers are often used for identifiers like:

// Port numbers

// Resource handles

// Session IDs

// Network addresses, etc.

// Your task is to implement an allocator that can:

// Allocate the lowest available number (i.e., smallest unused number).

// Release a number so it becomes available again for future allocation.

#include <stdio.h>
#include <stdlib.h>
IsBitSet()
int main()
{

    

    return 0;
}